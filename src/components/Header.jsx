import React from 'react';

/**
 * Renders the main application header.
 * @returns {JSX.Element}
 */
const Header = () => {
  return (
    <header className="app-header">
      <h1>The Podcast App</h1>
      <p>Discover, filter, and sort your favorite podcasts.</p>
    </header>
  );
};

export default Header;